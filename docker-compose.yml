services:
  assistant:
    build: .
    env_file: .env
    volumes:
      - ./data:/app/data
      - ./workspaces:/app/workspaces
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped
